import { ClickAwayDirective } from './click-away.directive';

describe('ClickAwayDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickAwayDirective(undefined);
    expect(directive).toBeTruthy();
  });
});
