import { ElementFocusDirective } from './element-focus.directive';

describe('ElementFocusDirective', () => {
  it('should create an instance', () => {
    const directive = new ElementFocusDirective();
    expect(directive).toBeTruthy();
  });
});
